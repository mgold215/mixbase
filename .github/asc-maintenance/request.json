{
  "action": "prune",
  "note": "2026-09-05: TestFlight run #26 hit 'account has reached the maximum number of certificates' — the widget-suite runs #24-26 each minted runner dev certs on every failed archive attempt (cert minting succeeds before the identifier step that was failing) and aged the account into the cap between monthly crons. Prune keeps the newest 4 'Created via API' DEVELOPMENT certs, same rails as the cron. Park back to action=list after the widget TestFlight upload is green."
}
