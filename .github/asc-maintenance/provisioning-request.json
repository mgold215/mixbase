{
  "action": "ensure-widgets",
  "note": "2026-09-05: TestFlight runs #24/#25 fail at GatherProvisioningInputs with 'Authentication failed' since the widget extension was added — cloud signing cannot register the new com.moodmixformat.mixbase.widgets bundle id or enable App Groups on the App IDs. Register/enable them here via the public ASC API so cloud signing returns to its long-working existing-identifier path. Idempotent; park back to action=none after it succeeds."
}
